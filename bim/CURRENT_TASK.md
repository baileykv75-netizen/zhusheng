# 当前任务：阶段6B——真实大模型增强模式与安全服务端网关

## 阶段6B状态说明

阶段6B只增加服务端DeepSeek Chat Completions适配器、本地回环网关和浏览器Provider，不修改IFC、Blend、GLB、建筑记忆种子或既有BIM业务身份。

真实模型仅负责自然语言结构化草稿、只读工具建议和确定性结果解释。诊断评分、状态跳转、人工授权、模拟设备动作、维修复验、成果包验证和集团评审仍由既有确定性模块与独立人工按钮负责。网关未配置或调用失败时，阶段6A确定性模式保持完整可用。

只有取得真实DeepSeek响应ID、JSON输出通过本地Schema且回归验证通过后，才能声明阶段6B完成；否则状态为“代码准备完成，等待真实API验收”。

> **脱敏合成演示模型与事件，不作为施工、诊断或设备控制依据。**
